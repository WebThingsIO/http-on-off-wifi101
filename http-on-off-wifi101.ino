// This file can be used when using the native Arduino IDE.
//
// Dependencies:
//  - Install Wifi101 using the Library Manager
//  - This also relies on ArduinoMDNS, which doesn't appear
//    to be available in the Library Manager. You can install it
//    locally by issuing the following commands from within your default
//    sketch directory:
//
//      cd ~/Arduino # Replace ~/Arduino with your default sketch directory
//      mkdir libraries # Only needed if the libraries subdirectory doesn't exist
//      cd libraries
//      git clone https://github.com/arduino-libraries/ArduinoMDNS.git
//
// When you build using the http-on-off-wifi101.ino file it should automatically
// pickup the src/main.cpp file (tested using Arduino 1.8.5 with an
// Board set to "Arduino/Genuino MKR1000"
